version: '3.5'
services:
  jupyter:
    image: jupyter/pyspark-notebook:x86_64-ubuntu-22.04
    command: start-notebook.sh --NotebookApp.token=''
    ports:
      - 8888:8888
    volumes:
      - .:/spark-project
